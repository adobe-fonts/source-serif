feature mark {
	include (../mark.fea);
} mark;


feature mkmk {
	include (../mkmk.fea);
} mkmk;


feature kern {
	lookupflag IgnoreMarks;
	lookup KERN useExtension {
		include (../kern.fea);
	} KERN;
	include (../kern_ctxt.fea);
} kern;
