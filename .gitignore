# ignore build folder:
target

# ignore font binaries in master branch:
Source*.otf
Source*.ttf

# ignore background, processed glyphs & hashmap in masters.
# Unfortunately, it is not possible to entirely ignore layers, because
# tracebacks are thrown when the layer folders are missing.
*/Masters/*/*.ufo/glyphs.com.adobe.type.processedGlyphs/*.glif
*/Masters/*/*.ufo/glyphs.com.adobe.type.processedGlyphs/contents.plist
*/Masters/*/*.ufo/glyphs.background/*.glif
*/Masters/*/*.ufo/glyphs.background/contents.plist
*/Masters/*/*.ufo/data/com.adobe.type.processedHashMap
